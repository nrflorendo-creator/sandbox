/**
 * @NApiVersion 2.1
 */
define(["../Library/lib/lib_series_number.js"], (lib) => {
  const create = (options) => {
    lib.autoGeneratedSeries(options);
  };

  const load = (options) => {
    lib.fldSetValue(options);
  };

  return { create, load };
});
